import { guideMetadata } from '@/lib/page-metadata'

export const metadata = guideMetadata('Theming', 'Customize omverse-ui components with semantic CSS variables and brand-level theme overrides.', '/docs/theming')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
