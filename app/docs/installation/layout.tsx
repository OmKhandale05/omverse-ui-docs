import { guideMetadata } from '@/lib/page-metadata'

export const metadata = guideMetadata('Installation', 'Install omverse-ui and configure its Tailwind CSS styles and application providers.', '/docs/installation')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
