import { guideMetadata } from '@/lib/page-metadata'

export const metadata = guideMetadata('Design tokens', 'Reference the color, typography, spacing, radius, and elevation tokens used by omverse-ui.', '/docs/design-tokens')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
