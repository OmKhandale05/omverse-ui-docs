import { guideMetadata } from '@/lib/page-metadata'

export const metadata = guideMetadata('Introduction', 'Learn what omverse-ui provides and how its components, tokens, and themes fit together.', '/docs/introduction')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
