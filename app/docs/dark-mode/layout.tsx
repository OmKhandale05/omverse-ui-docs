import { guideMetadata } from '@/lib/page-metadata'

export const metadata = guideMetadata('Dark mode', 'Configure class-based or system-aware dark mode for omverse-ui applications.', '/docs/dark-mode')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
