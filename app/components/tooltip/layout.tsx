import { componentMetadata } from '@/lib/page-metadata'
export const metadata = componentMetadata('Tooltip', 'Overlay', '/components/tooltip')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
