import { componentMetadata } from '@/lib/page-metadata'
export const metadata = componentMetadata('Card', 'Display', '/components/card')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
