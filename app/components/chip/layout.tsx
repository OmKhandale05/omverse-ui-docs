import { componentMetadata } from '@/lib/page-metadata'
export const metadata = componentMetadata('Chip', 'Display', '/components/chip')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
