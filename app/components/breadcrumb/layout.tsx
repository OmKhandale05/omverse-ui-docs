import { componentMetadata } from '@/lib/page-metadata'
export const metadata = componentMetadata('Breadcrumb', 'Navigation', '/components/breadcrumb')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
