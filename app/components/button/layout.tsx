import { componentMetadata } from '@/lib/page-metadata'
export const metadata = componentMetadata('Button', 'Form', '/components/button')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
